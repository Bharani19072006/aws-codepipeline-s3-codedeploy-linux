version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.9
    commands:
      - echo "Installing dependencies"
  build:
    commands:
      - echo "Build phase - nothing to compile"
  post_build:
    commands:
      - echo "Build completed"

artifacts:
  files:
    - '**/*'
